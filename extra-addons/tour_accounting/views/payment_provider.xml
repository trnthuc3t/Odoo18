<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_provider_form_inherit" model="ir.ui.view">
        <field name="name">payment.provider.form.inherit</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='messages']" position="after">
                <page string="Wire Transfer Config" name="wire_transfer_config" invisible="'Wire Transfer' not in name">
                    <group>
                        <field name="bank_account_number"/>
                        <field name="bank_name"/>
                        <field name="bank_account_holder" string="Bank Account Holder"/>
                        <field name="journal_id" widget="many2one" options="{'no_create': True}"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
