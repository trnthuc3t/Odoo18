<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_project_form_inherit" model="ir.ui.view">
        <field name="name">project.project.form.inherit</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='date']" position="after">
                <field name="order_id" widget="many2one" readonly="1" string="Sale Order"/>
            </xpath>
            <xpath expr="//field[@name='tag_ids']" position="replace">
                <field name="sale_tag_ids" widget="many2many_tags" readonly="1" string="Sale Tags"/>
            </xpath>
        </field>
    </record>
    <record id="view_project_tree_inherit" model="ir.ui.view">
        <field name="name">project.project.tree.inherit</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.view_project"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='display_name']" position="after">
                <field name="sale_tag_ids" widget="many2many_tags" readonly="1" string="Sale Tags"/>
            </xpath>
        </field>
    </record>
</odoo>
