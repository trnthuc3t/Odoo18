<odoo>
    <!-- Action for Lark Webhook -->
    <record id="action_lark_webhook" model="ir.actions.act_window">
        <field name="name">Lark Webhooks</field>
        <field name="res_model">lark.web.hook</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Lark Webhook
            </p>
        </field>
    </record>

    <!-- Menu Item: Lark Webhook, placed after base.menu_users -->
    <menuitem id="menu_lark_webhook_root"
              name="Lark Webhooks"
              parent="base_setup.menu_config"
              action="action_lark_webhook"
              sequence="98"/>

    <!-- Tree View -->
    <record id="view_lark_webhook_tree" model="ir.ui.view">
        <field name="name">lark.web.hook.tree</field>
        <field name="model">lark.web.hook</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="short_name"/>
                <field name="hook_url"/>
                <field name="hook_url_basic_key"/>
                <field name="is_active"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_lark_webhook_form" model="ir.ui.view">
        <field name="name">lark.web.hook.form</field>
        <field name="model">lark.web.hook</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="short_name"/>
                        <field name="hook_url"/>
                        <field name="hook_url_basic_key" password="True"/>
                        <field name="is_active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>